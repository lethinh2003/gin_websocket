# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
api

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspaces
go.work
go.work.sum

# Env files
.env
*.env

# IDEs / Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.history/

# Tooling / Temp folders
tmp/
.air.toml
# Note: Sometimes .air.toml is tracked, but tmp/ is definitely temporary build output of air.
