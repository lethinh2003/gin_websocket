version: "3.8"

services:
  # go_server:
  #   build: .
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - DATABASE_URL=host=postgres user=postgres password=root dbname=army_casino port=5432 sslmode=disable
  #     - JWT_SECRET=my_super_secret_key
  #   depends_on:
  #     - postgres
  #   restart: always

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=learning_go
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
